name: ingkung_mbah_oerip
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive: ^2.2.3        # Database Lokal
  hive_flutter: ^1.1.0
  path_provider: ^2.0.11 # Untuk menemukan path penyimpanan
  crypto: ^3.0.3     # Enkripsi
  http: ^1.1.0       # API / Web Service
  intl: ^0.18.1      # Konversi Waktu
  timezone: ^0.9.2   # State Management
  provider: ^6.0.5   # Untuk LBS
  location: ^5.0.0   # Untuk membuka Google Maps & link
  url_launcher: ^6.1.14  
  carousel_slider: ^5.0.0
  geocoding: ^2.1.0
  sqflite: ^2.3.3+1
  path: ^1.9.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  flutter_local_notifications: ^17.1.2
  image_picker: ^1.2.0
  cryptography: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/images/

